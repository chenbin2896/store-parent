server:
  port: 8802

spring:
  application:
    name: account
  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:9001/

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8901/eureka/
