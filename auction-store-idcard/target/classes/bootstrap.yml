server:
  port: 8821

spring:
  application:
    name: card
  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:9001/

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8901/eureka/
