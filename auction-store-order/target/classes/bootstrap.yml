server:
  port: 8811

spring:
  application:
    name: order
  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:9001/

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8901/eureka/
